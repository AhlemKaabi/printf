int (*parser(char *format))
{
	/* declare table of type match_conversion */
	/* loop through the format and check for % */
	/* if no % is found return NULL */
	/* if a match is found return a pointer to the matched function */
}